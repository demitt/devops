upstream php {
    server 192.168.0.11:9000;
}

server {
    listen 80;   # listen 443 ssl http2
    server_name myserver.com;
    # TODO: move "myserver.com" to variables; use template instead of file
    # TODO: sertificate

    location / {
        # TODO
        fastcgi_pass php;
    }

    location ~ /static/ {
        #rewrite ^.+/static/(.+)$ /war/www/html/static/$1;
        #root /war/www/html/static;

        # alias /war/www/html/static
    }
}
